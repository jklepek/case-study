export type Product  = {
    id?: number;
    name: string;
    quantity: number;
    pricePerUnit: number;
}

export type ProductFormData = {
    name: string;
    quantity: number;
    pricePerUnit: number;
} 
